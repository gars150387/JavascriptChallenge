/**
 * @param {number[]} arr
 * @return {number[]}
 */
 var replaceElements = function(arr) {
    let aux = []
    for (let outter = 0; outter < arr.length; outter++){
        // aux.push(arr[outter])
        // aux.reverse()
        console.log(arr)
        let swap = []

        if(arr[outter+1] > arr[outter] ){
           swap = arr[outter+1]
           let curr = arr[outter]
           let next = swap
            console.log(arr)
        }
        
    }

}